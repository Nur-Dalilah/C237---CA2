<!DOCTYPE html>
<html lang="en">
<head>
  <style>.nav-button {
  display: inline-block;
  background-color: #007BFF;   /* Blue background */
  color: white;                /* White text */
  padding: 10px 20px;          /* Button size */
  text-decoration: none;       /* Remove underline */
  border: none;
  border-radius: 5px;          /* Rounded corners */
  font-size: 16px;
  margin-top: 10px;
  transition: background-color 0.3s ease;
}

.nav-button:hover {
  background-color: #0056b3;   /* Darker blue on hover */
}

.welcome-text {
  font-size: 24px;             /* Larger text */
  font-weight: bold;
  display: block;
  margin-bottom: 10px;
}
</style>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Listing App</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand">Listing App</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
          <ul class="navbar-nav">
            <% if (!user) {%>
                <li class="nav-item">
                    <a class="nav-link" href="/register">Register</a>
                </li> 
                <li class="nav-item">
                    <a class="nav-link" href="/Login">Login</a>
                </li> 
            <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" href="/Logout">Logout</a>
                </li> 
            <% } %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
        <p style="text-align: center;">
            <% if (user) {%>
              <% if (user.role === "admin") { %>
                <br>
                Back to <a href="/inventory" class="nav-button">inventory!</a>
              <% } else { %>
                <br>
                Continue <a href="/shopping" class="nav-button">Shopping!</a>
                <% } %>
            <% } else { %>
            <br>
            <span class="welcome-text">Welcome to our app!</span> <br>
            Please login to continue...
            <a href = "/login" class="nav-button">login</a>
            <% } %>
        </p>
        
    </div>
</body>
</html>

